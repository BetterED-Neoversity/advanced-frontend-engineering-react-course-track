"use client";

export default function ClickCounter() {
  const counterValue = 0; // TODO: підключити до стора
  const increment = () => {}; // TODO: підключити до стора

  return <button onClick={increment}>{counterValue}</button>;
}
