# Заняття 2 — Події та стан компонента

## Обробка подій

- Іменування атрибутів обробників `on*`
- Додавання обробника події
- Посилання на функцію та анонімний колбек
- Об'єкт події
- Типізація об'єкта події

## Стан компонента

- Реактивність
- [Стан компонента](./src/assets/state.png)
- [Поняття **оновлення** та **рендера** компонента](./src/assets/component-update.png)
- Хук `useState`
- Обмеження хуків
- Асинхронність оновлення стану
- Локальність стану з `ClickCounter`
- [Підняття стану](./src/assets/lifting-state.png)
- Компоненти `Counter` та `CountDisplay`
- Декілька станів (зробити ефект toggle з `setIsVisible(!isVisible)`)
- Оновлення об'єктів

## Стартові файли у проєкті

- `src/components/ClickCounter.tsx` — компонент лічильника, потребує стану та обробника кліку
- `src/components/CountDisplay.tsx` — компонент відображення значення, отримує `count` через пропси після підняття стану

## Послідовність заняття

1. Додати обробник кліку до кнопки — пояснити різницю між посиланням на функцію та анонімним колбеком
2. Додати `useState` до `ClickCounter` — показати реактивність
3. Показати локальність стану — два `ClickCounter` незалежні
4. Підняти стан у `App` — передати `count` та `increment` через пропси до `ClickCounter` та `CountDisplay`
5. Декілька станів — додати `isVisible` з toggle ефектом
6. Оновлення об'єктів у стані
